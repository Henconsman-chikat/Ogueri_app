class User {
  final String id;
  final String email;
  // Add other user fields like name, address, etc.

  User({required this.id, required this.email});

  // Add a method to convert User object to JSON and vice versa
}
